export enum SettingsVersion {
  V1 = 1,
  V2 = 2,
}

export const CURRENT_SETTINGS_VERSION = SettingsVersion.V2;

export enum FileShareVersion {
  V1_0 = "1.0",
}

export const CURRENT_FILESHARE_VERSION = FileShareVersion.V1_0;
